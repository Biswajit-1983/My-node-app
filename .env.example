# LeadPulse CRM Environment Configuration
# Copy this file to .env and fill in your actual values

# Application Settings
NODE_ENV=production
PORT=3000

# Session Security (REQUIRED - Generate a strong random string)
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# WhatsApp Business API (Optional - for WhatsApp messaging features)
WHATSAPP_BUSINESS_API_KEY=your-whatsapp-business-api-key
WHATSAPP_BUSINESS_PHONE_ID=your-whatsapp-phone-id

# Database (Optional - currently using in-memory storage)
# DATABASE_URL=postgresql://username:password@localhost:5432/leadpulse

# Security Notes:
# 1. Never commit the actual .env file to version control
# 2. Use a strong, unique SESSION_SECRET (at least 32 characters)
# 3. Keep your WhatsApp API credentials secure
# 4. For production, consider using a real database instead of in-memory storage